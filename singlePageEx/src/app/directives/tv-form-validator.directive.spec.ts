import { TvFormValidatorDirective } from './tv-form-validator.directive';

describe('TvFormValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new TvFormValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
